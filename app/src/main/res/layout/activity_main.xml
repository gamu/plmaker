<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_height="match_parent"
    android:layout_width="match_parent"
    android:orientation="vertical"
    android:theme="@style/Base.Theme.PlaylistMaker">
    <androidx.fragment.app.FragmentContainerView
        android:id="@+id/container_view"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:name="androidx.navigation.fragment.compose.ComposableNavHostFragment"
        app:navGraph="@navigation/main_navigation"
        app:defaultNavHost="true" />
    <View
        android:id="@+id/bottomMainNavigationDelimiter"
        android:layout_width="match_parent"
        android:layout_height="0.5dp"
        android:background="@color/ypWhite" />
    <com.google.android.material.bottomnavigation.BottomNavigationView
        style="@style/MainBackground"
        android:id="@+id/bottomMainNavigation"
        android:layout_width="match_parent"
        android:layout_height="@dimen/size56"
        app:itemPaddingBottom="@dimen/emptySize"
        app:menu="@menu/main_bottom_menu" />
</LinearLayout>